{/*
import About from './componentsForDayFour/About';
import Contact from './componentsForDayFour/Contact';
import Home from './componentsForDayFour/Home';
import UserProfile from './componentsForDayFour/UserProfile';
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <Router>
      <div style={{ padding: 20, backgroundColor: 'lightgray', marginBottom: 20 }}>
        <Routes>
          <Route path='/' element={<Home />} />
          <Route path='/about' element={<About />} />
          <Route path='/contact' element={<Contact />} />
          <Route path='/user/:id' element={<UserProfile />} />
        </Routes>
      </div>
    </Router>
  )
}

export default App;
*/}

// Map Function Example

{/*
import React from 'react';
import MapUi from './componentsForDayFour/MapUi';

const App = () => {
  const college = [{ name: 'Harvard', location: "afhghshf" },
  { name: 'Stanford', location: "xgsgj" },
  { name: 'MIT', location: "xhfsfu" },
  { name: 'Yale', location: "sxjgjxg" },
  { name: 'Princeton', location: "xjgjxg" }];

  return (

    <div>
      <h1>these are college names</h1>

      {college.map(val => {
        console.log(val);
        return <MapUi key={val.name} kuchhbhi={val.name} orkuchh={val.location} />
      })}
    </div>
  );
};


export default App;
*/}